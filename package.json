{
  "name": "boring-cypress",
  "version": "0.1.0",
  "private": false,
  "main": "dist/BoringCypress.js",
  "module": "dist/BoringCypress.js",
  "files": [
    "dist",
    "README.md"
  ],
  "babel": {
    "presets": [
      "@babel/react",
      "@babel/preset-env"
    ]
  },
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.4.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build": "rm -rf dist && NODE_ENV=production npx babel src/lib --out-dir dist --copy-files",
    "publish": "npm run build && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leandrotk/boring-cypress"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
