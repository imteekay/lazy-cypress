{
  "name": "lazy-cypress",
  "version": "0.2.0",
  "private": false,
  "main": "dist/LazyCypress.js",
  "module": "dist/LazyCypress.js",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "babel": {
    "presets": [
      "@babel/react",
      "@babel/preset-env"
    ]
  },
  "dependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.4.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "@babel/cli": "^7.8.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build": "rm -rf dist && NODE_ENV=production npx babel src/lib --out-dir dist --copy-files",
    "publish": "npm run build && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/leandrotk/lazy-cypress"
  },
  "homepage": "https://github.com/leandrotk/lazy-cypress",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "keywords": [
    "cypress",
    "testing",
    "integration testing",
    "react"
  ]
}
